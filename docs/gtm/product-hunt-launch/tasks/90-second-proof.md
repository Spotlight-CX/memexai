# Add 90-Second Proof Promise to Maker Comment and Timeline
**Priority:** P2
**Type:** copy-update
**Status:** [x] done

## Promise
The launch proof should be concrete: a new user can start Docker, write one memory, inspect it in admin, and run a second call that changes because the memory exists in about 90 seconds.

## Maker Comment Draft
MemexAI is inspectable memory for AI agents, backed by your Postgres. The 90-second proof is simple: run Docker, tell an agent one durable preference, open the admin UI to inspect the memory file and revision, then ask a follow-up that changes because the memory exists.

## Timeline Placement
Use this immediately after the one-line product description in Product Hunt comments, HN comments, and founder replies. It turns the claim into a testable loop.

## Verification
- Added the 90-second proof language to `README.md`.
- Added the proof promise to `docs/gtm-content-plan.md`.
